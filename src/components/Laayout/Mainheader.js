import Navigation from "./Navigation";

const Mainheader = () => {
    return (
        <header>
            <h1>Manage Allowance</h1>
            <Navigation />
        </header>
    );
};

export default Mainheader;
